%CHCEMY ZROBIC TAK ZEBY 500ms przed sakada był spokoj, tzn zeby  przek
%wydarzeniem które jest L_saccade_10,11,12,13 nie było ruchów oka z danych
%z et - > tym razem trzeba spojrzec na dane z et
function [dataset] = clean_et(dataset, odl)

%odl = 500
for ind = false(1,size(dataset.data, 3));
if |L_saccade_10 - L_saccade| > odl ms && |L_blink- |< odl
        ind(i) = true;
    end
end

dataset.epoch = dataset.epoch(ind);
dataset.data = dataset.data(:,:,ind);